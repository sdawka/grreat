---
export interface Props {
  bucket: 'goals' | 'research' | 'roadmap' | 'execution' | 'analysis' | 'time';
  letter: string;
  name: string;
  desc: string;
  class?: string;
}

const { bucket, letter, name, desc, class: className, ...rest } = Astro.props;
---

<li class:list={['bucket-card', className]} data-bucket={bucket} {...rest}>
  <span class="letter">{letter}</span>
  <span class="name">{name}</span>
  <p class="desc">{desc}</p>
</li>

<style>
  .bucket-card {
    background: var(--surface-raised);
    padding: var(--space-6);
    transition: background var(--duration-fast);
  }
  .bucket-card:active {
    background: var(--surface-page);
  }

  .bucket-card[data-bucket='goals'] { --bucket-accent: var(--bucket-goals); }
  .bucket-card[data-bucket='research'] { --bucket-accent: var(--bucket-research); }
  .bucket-card[data-bucket='roadmap'] { --bucket-accent: var(--bucket-roadmap); }
  .bucket-card[data-bucket='execution'] { --bucket-accent: var(--bucket-execution); }
  .bucket-card[data-bucket='analysis'] { --bucket-accent: var(--bucket-analysis); }
  .bucket-card[data-bucket='time'] { --bucket-accent: var(--bucket-time); }

  .letter {
    display: block;
    font-family: var(--serif);
    font-size: var(--text-numeral);
    line-height: var(--leading-none);
    letter-spacing: var(--tracking-display);
    color: var(--bucket-accent);
    margin-block-end: var(--space-2);
  }

  .name {
    display: block;
    font-weight: 600;
    font-size: var(--text-2);
    line-height: var(--leading-snug);
    text-transform: uppercase;
    letter-spacing: var(--tracking-caps-1);
    margin-block-end: var(--space-2);
  }

  .desc {
    margin: 0;
    font-size: var(--text-2);
    line-height: var(--leading-relaxed);
    color: var(--text-muted);
  }
</style>
